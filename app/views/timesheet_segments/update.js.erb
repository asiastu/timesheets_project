function removeForm(form){
  console.log(event.target)
  form.style.display = 'none';
}

function showInput(currentForm) {
  // currentForm.submit()
  document.getElementById("timesheetsegment<%= @timesheetsegment.id %>").innerHTML = "<%= j render 'timesheet_segments/info', timesheetsegment: @timesheetsegment %>"
    // document.getElementById("user_input").value;

}

// document.querySelectorAll('.edit_timesheet_segment').forEach(function(form) {
//   form.addEventListener('submit', (event)=> {
//     createCard(event);
//     showInput();
//   });
// });


document.querySelectorAll(".testben").forEach((btn) => {
  btn.addEventListener("click", (e) => {
    currentForm = btn.parentNode
    removeForm(currentForm);
    showInput(currentForm);
    // console.log(currentForm);
  })
})
